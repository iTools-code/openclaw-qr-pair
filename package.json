{
  "name": "openclaw-qr-pair",
  "version": "1.0.0",
  "description": "Generate mobile pairing QR code for OmniClaw iOS App",
  "main": "dist/index.js",
  "bin": {
    "openclaw-relay": "dist/relay.js"
  },
  "scripts": {
    "build": "tsc",
    "prepack": "npm run build"
  },
  "openclaw": {
    "extensions": [
      "dist/index.js"
    ]
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/qrcode-terminal": "^0.12.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.0.0"
  }
}
